import styled from 'styled-components';

const Wrapper = styled.div`
  p {
    font-size: 1.1rem;
    line-height: 2;
    color: var(--grey-500);
    margin-top: 2rem;
  }
`;

export default Wrapper; 